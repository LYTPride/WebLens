# 资源列表：筛选、排序与实时更新

本文说明 WebLens 中 **Pods**、**Deployments**、**StatefulSets**、**PersistentVolumeClaims（PVC）**、**Events** 等资源表格的共用列表能力（其他资源类型后续可按同一套能力扩展）。**Ingress**、**Services** 列表同样支持 Name 筛选、表头调宽与排序等（以当前版本为准）；展开区与 Describe 中的名称与联动交互见 [Ingress 与 Services](./ingress-services.md)。**Events** 的默认排序策略与 Involved Object 跳转约定见 [Events 使用手册](./events.md)。

## 列表标题格式

- 各资源页主标题为 **「资源类型 · 命名空间 / 当前条数」**，例如：`Pods · train-uat / 107`；**Nodes** 等无命名空间范围时仅显示类型与条数（命名空间列表由顶部「集群与命名空间」作用域提供，不再单独占一页）。
- **集群、作用域别名、kubeconfig 显示名** 等完整上下文在上方 **「集群与命名空间 · 当前：…」** 小字中展示，**不再**重复写在标题括号内，以免长集群 ID 挤占横向空间、把右侧提示与 Name 过滤框顶换行。

## 关键字筛选

- 顶部 **按 Name 关键字过滤** 仅影响当前表格展示，不改变已应用的集群与命名空间范围。

## 表头列宽（主表与行内展开子表）

- 多数资源列表的表头单元格右侧可 **拖拽调整列宽**（与主表深色风格一致）。
- **行内展开** 的资源（如 **Ingress** 规则排障子表、**StatefulSet** 实例 Pod 子表、**Services** 展开区的端口与 Endpoints 子表）在展开后，子表表头同样支持 **拖拽调宽**；子表较宽时仅在展开区域内出现 **横向滚动条**，避免挤压整页布局。
- 子表单元格内长文本（Host、Path、Service 名、Pod 名、说明等）会在 **格内自动换行**，行高随内容增高，减少窄屏下的阅读错乱。
- 列宽调整在当前浏览器会话内有效（刷新页面后恢复默认宽度）。

## 排序

- 部分表头右侧有 **▲ / ▼**，分别表示按该列 **升序 / 降序**；同一时间只有 **一列** 排序生效。
- 未点击排序时：多数资源列表顺序与 **数据到达顺序** 一致（不经额外排序）；**Events** 例外，使用 **异常优先默认顺序**，见上文 [Events（事件）](#events事件) 与 [Events 使用手册](./events.md)。
- **切换左侧资源页**（如 Pods ⇄ Deployments）时，**各自记住** 上次的排序方式。
- 点击 **「刷新列表」** 会 **清空当前页的排序**，并重新拉取该页数据。

## 与 Watch 的关系

- 列表在首次加载或「刷新列表」后，会由 **Watch** 持续推送增量更新（新增/修改/删除），无需依赖高频轮询即可接近准实时。
- 若你已开启排序，界面会 **按当前排序规则持续重排**，以便反映最新状态。
- 在 **Deployments** 等页面时，后台仍会保持与当前作用域相关的 **Pods** 数据更新（便于 rollout 后切回 Pods 不滞后）；具体策略见开发文档 [资源列表数据流](../dev/resource-list-dataflow.md)。

## Events（事件）

- **Events** 为 **命名空间作用域** 资源：**HTTP list + watch**、Name（Involved）筛选、表头调宽与列排序、**serverTimeMs** 与 **Age** 列行为与其他列表一致。
- **未开启列排序** 时，表格使用 **异常优先的默认顺序**（Warning、count、lastSeen 等综合排序），便于排障；点击表头列后则按该列升/降序，与「刷新列表清空排序」规则一致。详情见 [Events 使用手册](./events.md)。

## PersistentVolumeClaims（PVC）

- **PVC** 为 **命名空间作用域** 资源：列表受当前已应用的 **集群 + 命名空间** 约束（与 Pods、Deployments 等一致），采用与其他资源相同的 **HTTP list + watch** 模式。
- 支持 **Describe** 侧栏、**YAML 编辑**（底部面板标签页）与 **删除**（需集群 RBAC 允许）；具体列字段与状态摘要以界面为准。
- 若当前身份对 PVC 无 list/watch 等权限，行为与常规资源页一致（错误提示以实际 API 返回为准）；后续也可按 **Nodes** 同款 **`ResourceAccessDeniedState` + `resourceAccessCache`** 扩展受限态（见 [Nodes 与访问权限](#nodes-与访问权限rbac)）。

## Age（存活时间）列

- **Age** 由资源的 `metadata.creationTimestamp` 与 **服务端时间** 共同计算：后端在 list 与 watch 中会附带 **`serverTimeMs`**，界面按该时间基准展示，而不是单纯使用你电脑的系统时间，以便与集群、`kubectl get` 的观感更一致。
- 若你 **人为修改了本机时间** 或服务端与浏览器时间差较大，列表顶部可能出现轻量提示：*本地时间与集群时间存在偏差，Age 已按服务端时间校准*；此时 Age 仍以服务端基准为准。
- 极端时钟异常下 Age 不会用 `"-"` 表示「负时长」，而是 **从 0s 起显示** ，避免误以为是数据缺失。

## 排序位置变化提示

- **仅当当前已开启排序时**：若某行因数据更新导致其在 **排序结果中的行序** 发生变化，该行会出现 **短暂的浅色高亮**（约 1.5 秒内淡出）。
- 若数据有变化但 **排序位置未变**，不会出现该提示。
- 未开启排序时，不会有此类提示。

## Nodes 与访问权限（RBAC）

- **Nodes** 是 **集群级（cluster-scoped）** 资源，许多集群只为命名空间级角色授予权限；若当前身份 **无权 list/watch Nodes**，WebLens **不会**用整页红色错误栈打断你，而是在列表主区域显示与产品风格一致的 **受限态**（标题如「当前身份无权查看 Nodes」、说明如何联系管理员授权）。
- **技术摘要**（若有）默认折叠，需要时再展开查看短摘要，避免把原始 RBAC 长文直接铺在页面上。
- 同一 **已选集群** 下，若已判定为无权限，会 **减少重复的 list/watch 与错误提示**；换集群或希望重新探测时，点击 **「刷新列表」** 会清除该资源在本集群的探测缓存并再次请求。
- 侧栏 **仍保留 Nodes 入口**：有权限时与往常一样使用列表；无权限时进入即为受限态，便于后续 RBAC 放开后无需改代码即可使用。

## 相关手册

- [Pods 使用手册](./pods.md)
- [Deployments 使用手册](./deployments.md)
- [Events 使用手册](./events.md)
